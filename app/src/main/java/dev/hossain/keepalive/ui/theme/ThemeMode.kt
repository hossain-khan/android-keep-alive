package dev.hossain.keepalive.ui.theme

/**
 * Enum representing the available theme modes for the application.
 */
enum class ThemeMode {
    /** Light theme mode */
    LIGHT,

    /** Dark theme mode */
    DARK,

    /** System theme mode - follows system setting */
    SYSTEM,
}
